storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=upload
storeFile=../keystore.jks

# Instructions:
# 1. Create this file as 'key.properties' (remove .example)
# 2. Generate keystore: keytool -genkey -v -keystore keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
# 3. Fill in your passwords
# 4. Add key.properties to .gitignore (DO NOT commit passwords!)

